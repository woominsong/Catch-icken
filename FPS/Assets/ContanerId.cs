using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ContanerId : MonoBehaviour
{
    // Start is called before the first frame update

    public int contanerId;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
